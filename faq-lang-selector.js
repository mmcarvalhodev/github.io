(function () {
  // No-op: language pill bar removed (duplicate of navbar switcher).
  // Hash-preserving language navigation is handled by lang-switcher.js.
  // "See more FAQs" link is visible in all languages via the HTML.
})();
